我们现在需要一个脚本，对Resources/Templates/gles3/1_decompressed.byte进行修改，
他是一个二进制文件，加载他的方式是Resources.Load<TextAsset>(path).bytes，

现在我需要说明该文件的部分格式
有什么问题不要猜，及时停下来问我

0000~0003
该文件中的模块数量，模块以BA 75 0A 0C开头，这是一个时间（后续也写这个）
看起来有两个模块的信息

紧跟着的4字节(0004~0007)，为第一个模块的偏移(0000+001C)
再后面紧跟着的4字节(0008~000B)，为第一个模块的大小(001C+00D0)
由此可得第一个模块的范围是001C~00EB
再后面紧跟着的4字节(000C~000F)，为第一个模块信息的terminator

再后面12个字节为第二个模块的信息

接下来说明第一个模块实际内容的格式
这部分了解不太深入，我们只看我们已知的部分

我们先跳过BA 75 0A 0C这个开头
再跳过20个字节（0020~0033），这部分功能未知，不改动他

然后是一堆CB Entries
第一个CB Entries是片元着色器的
格式看起来就像这样

--- CB Entries ---
  Entry format: [4]name_len, [name], [pad], [4]cbSize, [4]cbIndex, [4]cbOffset
  0x0034  "$Globals"  cbSize=24, cbIndex=0, cbOffset=0
  0x004C  "$Globals"  cbSize=144, cbIndex=3, cbOffset=11

你可以验证一下，不过我们只改片元着色器，即0040~0043
毕竟我们添加和减少uniform不会修改后面的cbIndex和cbOffset，只会对大小造成影响

那么第一个模块需要修改的内容已经说明完毕，现在我们来看第二个模块

我们先跳过BA 75 0A 0C这个开头
再跳过24个字节（00F0~0107），这部分功能未知，不改动他

然后紧跟的4个字节(0108~010B, 2361个字节)着色器部分的尺寸

注意，着色器最末尾有pad对齐(0A45~0A47)

然后是最后的11 00 00 00 00 00 00 00 这部分功能位置，原样誊写

片元cb的大小(int)
第二个模块内的glsl文本（现在的范围是010C~0A44）